* The antenna is commanded every 100 msec.  If the integration time is
  1 sec, then there will be ten pointings per integration.

* Do the LO1B files contain a test tone?
  -> are the LO1B data stored in the DATA structure?
     -> if no, why read them in?
  -> are these data at all useful?
  -> are the LO1B data always saved?

* Is there some way to determine which directories exist simply from
  the elements in ScanLog.fits?  This should be the general way to go!

* How do we get the AZ, EL?
