routines for ter simulation

axisprof -	defines fixed axis parameters
tercmp   - do a computation run
@terinit - call once at startup
tersetup - define dynamic pi parameters
terStep  - compute change in 1 5ms step


calling sequence

@terinit		- define structure .. call once


tersetup - with freq,damping, axis etc


tercmp - does the computation

setup.datdef .. default datafile used by tersetup.

dacalg.pro - dac algorithm dac value to voltage output.
